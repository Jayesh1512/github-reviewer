/** Pull surrounding lines from full file text for AI context — placeholder. */
export function extractSurroundingLines(
  _fullFile: string,
  _startLine: number,
  _endLine: number,
  _padding?: number,
): string {
  return '';
}
