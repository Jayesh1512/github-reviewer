/** Map diff rows to old/new file line numbers — expand when posting GitHub comments. */
export function placeholderLineMap(): Record<number, { old: number | null; new: number | null }> {
  return {};
}
